
import SelectElement from '../components/SelectElementType/inline';
import {compose} from 'recompose';

const enhance = compose(
    
);
export const PlanBuilderSelectElement = enhance(SelectElement);