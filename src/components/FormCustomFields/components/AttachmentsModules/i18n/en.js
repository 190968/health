import { defineMessages } from 'react-intl';

export default defineMessages({
    'noAttachments': {
        "id": "task.attachments.empty",
        "defaultMessage": "No Attachments",
        "description": "No Attachments"
    }
})
